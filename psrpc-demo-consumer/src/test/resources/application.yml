server:
  port: 8090

psrpc:
  zk:
    server: localhost:2182
    root: psrpc
  app:
    id: psrpc
    namespace: public
    env: dev
    version: 1.0.0
  consumer:
    retries: 2
    timeout: 1000

logging:
  level:
    root: error
    cn.pengshao.rpc: info